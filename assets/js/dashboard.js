// Placeholder for future interactivity (fetch data, update bins, etc.)
console.log("Dashboard loaded");

// Example: click to show bin details
document.querySelectorAll(".details-btn").forEach(btn => {
  btn.addEventListener("click", () => {
    alert("Bin details page coming soon!");
  });
});
